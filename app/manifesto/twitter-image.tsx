// Twitter card — same design as opengraph-image. Re-export to keep
// the two in lockstep without a parallel implementation.
export {
  default,
  runtime,
  alt,
  contentType,
  size,
} from "./opengraph-image";
